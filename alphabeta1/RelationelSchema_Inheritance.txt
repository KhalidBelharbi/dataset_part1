
groupingviewrecord(view:Grouping.View [PK], strategy:GroupingLevelRecord [PK] #FK to(groupinglevelrecord));

groupinglinkrecord(strategy:GroupingLevelRecord [PK] #FK to(groupinglevelrecord), strategy:GroupingLevelRecord [PK] #FK to(groupinglevelrecord));

groupinglevelrecord(strategy:Grouping.Strategy [PK], name:Grouping.Name [PK]);


** PROGRAM TYPE: JPA+Hibernate(Hybrid)