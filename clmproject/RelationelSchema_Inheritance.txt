
teacher(id:String [PK], name:String);

user(//@pattern(regexp:String [PK], username:String, password:String);

standard(//@generatedvalue表示sid为一个自增长字段，也就不需要设置set方法了id:String [PK], version:String [PK], name:String);

wc_table(id:Integer [PK], word:String, count:Integer);

menu(id:int [PK], menuname:String);

role(id:int [PK], role_name:String);


** PROGRAM TYPE: JPA+Hibernate(Hybrid)